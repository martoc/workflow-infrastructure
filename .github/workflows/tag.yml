---
name: Tag and Release

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  github-actions:
    uses: martoc/workflow-github-actions/.github/workflows/github-actions.yml@main
    secrets: inherit
